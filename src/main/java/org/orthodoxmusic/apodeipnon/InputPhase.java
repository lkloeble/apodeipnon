package org.orthodoxmusic.apodeipnon;

import static org.orthodoxmusic.apodeipnon.InputType.*;

public class InputPhase {

    /*
    private InputType currentType;

    public InputPhase() {
        currentType = TEXT;
    }

    public void setCurrentType(String keyboardInput) {
        InputType inputType = InputType.getValueByKeyboardInput(keyboardInput);
        currentType = UNKNOWN.equals(inputType) ? currentType : inputType;
    }

    public InputType getCurrentType() {
        return currentType;
    }

    public boolean isTextPhase() {
        return currentType.equals(TEXT);
    }

    public boolean isNeumesPhase() {
        return currentType.equals(NEUMES);
    }

    public boolean isNewBlockPhase() { return currentType.equals(NEW_BLOCK);  }

    public void setTextPhase() {
        currentType = TEXT;
    }
    */
}
